# Streamlit-Cloud-friendly requirements (lean + pinned major versions)
# Selenium/webdriver-manager removed — only needed for scraping locally.
# Install for local dev: pip install -r requirements.txt

# Core data
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# ML
scikit-learn>=1.3.0
xgboost>=2.0.0
statsmodels>=0.14.0

# App + visualization
streamlit>=1.28.0
pydeck>=0.8.0
altair>=5.0.0

# Optional: only needed if you re-scrape locally
# beautifulsoup4
# requests
# selenium
# webdriver-manager
